fastapi>=0.99.0
uvicorn[standard]>=0.20.0
python-dotenv>=1.0.0
requests>=2.28.0
httpx>=0.24.0
psycopg2-binary>=2.9.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
# chromadb>=0.5.0  # Optional: vector store for RAG. Install separately if needed;
# the app uses a pure-Python TF-IDF fallback by default.
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
playwright>=1.40.0      # Required for CI E2E tests; on Windows install via: pip install playwright --no-build-isolation
pytest-playwright>=0.4.0  # Required for CI E2E tests
mypy>=1.11.0,<2.0  # Pin <2.0 on Windows (2.x needs Rust for ast-serialize build)
types-requests>=2.32.0
types-python-dateutil>=2.9.0
